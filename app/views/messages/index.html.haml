-# %header
-#   グループを作成しました
.wrapper
  -# = render 'shared/side_bar'
  .wrapper__side
    .wrapper__side__header
      %p.wrapper__side__header__name
        = current_user.name
      %a.wrapper__side__header__lists
        = link_to edit_user_path(current_user) do
          = fa_icon 'cog', class: 'icon'
        = link_to new_group_path do
          = fa_icon 'pencil-square-o', class: 'icon'
    .wrapper__side__groups
      - current_user.groups.each do |group|
        .wrapper__side__groups__room
          = link_to group_messages_path(group) do
            .wrapper__side__groups__room__name
              = group.name
            .wrapper__side__groups__room__message
              = group.show_last_message
  .wrapper__main
    .wrapper__main__header
      .wrapper__main__header__contents
        %h2
          test
        %p
          Member : KuriY
      .wrapper__main__header__edit
        #box
          %p
            Edit
    .wrapper__main__body
      .wrapper__main__body__chat
        = render @messages
      -# = render partial: 'message', collection: @messages
      -# .wrapper__main__body__chat
      -#   %p.wrapper__main__body__chat__comment__name
          -# = %message.user.name
          -# KuriY
          -# %p.wrapper__main__body__chat__comment__time
            -# = message.created_at.strftime("%Y/%m/%d %H:%M")
            1900/1/1
        -# %p.wrapper__main__body__chat__comment__text
          -# - if message.content.present?
          -#   %p.lower-message__content
              -# = message.content
          -# = image_tag message.image.url, class: 'lower-message__image' if message.image.present?
    .wrapper__submit
      .wrapper__submit__text
        = form_for [@group, @message] do |f|
          = f.text_field :content, class: 'textbox', placeholder: 'type a message'
          #i.form__mask
            = f.label :image, class: 'form__mask__image' do
              = fa_icon 'picture-o', class: 'icon'
              = f.file_field :image, class: 'hidden'
              -# .wrapper__submit__send
          = f.submit 'Send', class: 'box'

        -# = text_area_tag :text, '', placeholder: 'type a message', class: "textbox"
      -#   .form__mask
      -#     %label.upload-label{for: "upload-icon"}
      -#       %i.fa.fa-picture-o
      -#       %input{type:"file",style:"display:none;",id:"upload-icon"} 
      -# .wrapper__submit__send
      -#   #box
      -#     %p
      -#       Send